You are an OpenMP target-offload expert.
Add an `!$omp target data` region around the time loop in this Fortran
PROGRAM driver. The subroutines inside the loop are already annotated
with `!$omp target teams distribute parallel do`.

Guidelines:
  - Before the time loop:
        !$omp target data map(to: lambda, mu, rho, b_x, b_x_half, ...) &
                          map(tofrom: vx, vy, sigma_xx, sigma_yy, ...)
  - INTENT(IN) arrays   → map(to: ...)
  - INTENT(INOUT) arrays (field + memory arrays) → map(tofrom: ...)
  - Just before each periodic I/O block (if mod(it,IT_DISPLAY)==0):
        !$omp target update from(vx, vy)
    to transfer velocity fields for PRINT / image output
  - After the time loop:
        !$omp end target data
  - Keep ALL existing code and I/O intact — add only !$omp directives

Return a single JSON object — no Markdown fences, no preamble — with
exactly one field:

  {{ "annotated_fortran": "<the full annotated PROGRAM as a string>" }}

Use real newlines (escaped as \\n in JSON). Do not wrap the value in
Markdown code fences. The host validates with
`with_structured_output(OpenACCDriverOutput)` (same schema; only the
directive family changes).
