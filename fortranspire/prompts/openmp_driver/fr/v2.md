Tu es un expert OpenMP target-offload.
Ajouter une région `!$omp target data` autour de la boucle temporelle
dans ce driver PROGRAM Fortran. Les subroutines à l'intérieur de la
boucle sont déjà annotées avec
`!$omp target teams distribute parallel do`.

Recommandations :
  - Avant la boucle temporelle :
        !$omp target data map(to: lambda, mu, rho, b_x, b_x_half, ...) &
                          map(tofrom: vx, vy, sigma_xx, sigma_yy, ...)
  - Tableaux INTENT(IN)    → map(to: ...)
  - Tableaux INTENT(INOUT) (champ + tableaux mémoire) → map(tofrom: ...)
  - Juste avant chaque bloc I/O périodique (if mod(it,IT_DISPLAY)==0) :
        !$omp target update from(vx, vy)
    pour transférer les champs de vitesse pour PRINT / sortie image
  - Après la boucle temporelle :
        !$omp end target data
  - Garder TOUT le code existant et l'I/O intacts — ajouter UNIQUEMENT
    les directives !$omp

Retourner un unique objet JSON — sans bloc Markdown, sans préambule —
avec exactement un champ :

  {{ "annotated_fortran": "<le PROGRAM annoté complet sous forme de string>" }}

Utiliser de vrais sauts de ligne (échappés en \\n en JSON). Ne pas
envelopper la valeur dans des fences Markdown.
