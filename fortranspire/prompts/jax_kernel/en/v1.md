You translate one Fortran routine into a JAX function.

The functional interface has already been derived from the Fortran AST.
It is not a suggestion — emit exactly this signature:

```
{signature}
```

Fortran mutates its arguments; JAX cannot. Every `INTENT(OUT)` and
`INTENT(INOUT)` argument therefore comes back as a return value, which is
why the signature looks the way it does. Do not add, remove, reorder or
rename a parameter.

## Rules

1. Return a new value. Never mutate an argument. For an indexed write, use
   `x.at[i].set(v)` / `.add(v)`, which returns a new array.
2. A loop whose iterations are independent becomes a whole-array `jnp`
   expression, not a Python `for`.
3. A loop that carries a dependency is a recurrence: use `jax.lax.scan`.
   A vectorised expression would compute a different answer, silently.
4. **Any branch on a runtime value is `jnp.where` / `lax.switch`, never `if`.**
   A Python `if`/`elif` on a value the caller passes in — *or on a scalar read
   from an array* (`n = COUNT[i]; if n == 1`) — fails under `jit`: that value is
   traced, not concrete. Two-way choice → `jnp.where(cond, a, b)`. Multi-way on
   a small integer (`if k==1 … elif k==2 …`) → `jax.lax.switch(k, [f0, f1, …])`,
   or compute every branch and select with nested `where`. Only a value fixed at
   trace time — an array *shape* or a loop *bound* — may drive a Python `if`.
5. **Guard the untaken branch.** `jnp.where(x > 0, sqrt(x), 0.0)` evaluates
   `sqrt(x)` for negative `x` too, so the gradient is NaN even though the
   forward value is fine. Make the unsafe branch safe first:
   `sqrt(jnp.where(x > 0, x, 1.0))`.
6. Keep the declared array rank and index order. The caller relies on the
   Fortran layout.
7. No `print`, no file I/O, no global state.
8. Preserve the numerical formulation exactly — same operations, same
   order where it affects rounding. This is a translation, not a
   reimplementation.

## What the analysis found

{hints}

## Differentiability — smoothing mode: {smoothing_mode}

{smoothing_instruction}

Non-smooth constructs and their replacements, from
`fortranspire.jax_smooth`:

{smoothing_catalogue}

Import what you use — `from fortranspire.jax_smooth import smooth_max` —
rather than writing the expression yourself. The library forms are
numerically stable; the textbook softmax `log(exp(b*a) + exp(b*b))`
overflows for arguments past about 709/b.

## Fortran source

```fortran
{fortran_code}
```

Emit a single self-contained Python module: the `import` lines, then the
function. No prose, no explanation, no example call.
