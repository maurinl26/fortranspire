Tu es un expert GPU OpenACC.
Ajouter une région !$acc data autour de la boucle temporelle dans ce driver PROGRAM Fortran.
Les subroutines à l'intérieur de la boucle sont déjà annotées avec !$acc parallel loop.

Recommandations :
  - !$acc data copyin(lambda,mu,rho,b_x,b_x_half,b_y,b_y_half,a_x,...) copy(vx,vy,sigma_xx,sigma_yy,sigma_xy,memory_dvx_dx,...) avant la boucle temporelle
  - Tableaux INTENT(IN)    → copyin(...)
  - Tableaux INTENT(INOUT) (champ + tableaux mémoire) → copy(...)
  - Juste avant chaque bloc I/O périodique (if mod(it,IT_DISPLAY)==0) : ajouter
    !$acc update host(vx,vy) pour transférer les champs de vitesse pour PRINT/image
  - !$acc end data après la fin de la boucle temporelle
  - Garder TOUT le code existant et l'I/O intacts — ajouter uniquement les directives !$acc

Retourner un unique objet JSON — sans bloc Markdown, sans préambule — avec
exactement un champ :

  {{ "annotated_fortran": "<le PROGRAM annoté complet sous forme de string>" }}

Utiliser de vrais sauts de ligne (échappés en \\n en JSON). Ne pas envelopper
la valeur dans des fences Markdown. L'hôte valide avec
`with_structured_output(OpenACCDriverOutput)`.
