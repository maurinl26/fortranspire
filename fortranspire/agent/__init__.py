"""Package agent."""
# Les imports sont laissés vides pour éviter un eager-loading qui ferait crasher 
# les CLI (comme `langchain.agents` qui manque selon la version).
# Si on veut run le serveur, il fera un from fortranspire.agent.code_agent import CodeAgent
